name: Apply mobile layout follow-up

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/apply-mobile-layout-fix2.yml'
      - 'tools/mobile-layout-fix2.css'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - name: Append mobile layout follow-up
        shell: bash
        run: |
          if grep -Fq '/* V14 mobile layout follow-up — 2026-09-17 */' assets/css/app.css; then
            echo 'Mobile layout follow-up already present.'
          else
            printf '\n' >> assets/css/app.css
            cat tools/mobile-layout-fix2.css >> assets/css/app.css
            printf '\n' >> assets/css/app.css
          fi

      - name: Validate
        run: |
          node tools/check-html.mjs index.html
          node tools/check-v14-structure.mjs
          git diff --check

      - name: Commit mobile layout follow-up
        shell: bash
        run: |
          git add assets/css/app.css
          if git diff --cached --quiet; then
            echo 'No CSS change needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix: improve mobile navigation and compact sections'
          git push origin HEAD:dev
