name: Apply Stage 2e workspace extraction

on:
  push:
    branches: [dev]
    paths:
      - 'tools/apply-stage2e-workspace.mjs'
      - '.github/workflows/apply-stage2e-workspace.yml'

permissions:
  contents: write

jobs:
  extract-workspace:
    runs-on: ubuntu-latest
    steps:
      - name: Check out dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Extract workspace runtime
        run: node tools/apply-stage2e-workspace.mjs
      - name: Validate resulting application
        run: node tools/check-html.mjs index.html
      - name: Verify Stage 2e.3 structure
        shell: bash
        run: |
          test -s assets/js/workspace.js
          grep -q 'src="assets/js/workspace.js"' index.html
          grep -q 'function renderWorkspace()' assets/js/workspace.js
          grep -q 'function openCmd()' assets/js/workspace.js
          grep -q 'const LEGAL_PACK_KEY=' assets/js/app.js
          if grep -q 'function renderWorkspace()' assets/js/app.js; then
            echo 'workspace runtime still exists in app.js' >&2
            exit 1
          fi
          git diff --check
      - name: Commit Stage 2e.3 extraction
        shell: bash
        run: |
          git add index.html assets/js/app.js assets/js/workspace.js
          if git diff --cached --quiet; then
            echo 'No Stage 2e.3 changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'refactor: isolate workspace and command palette runtime'
          git push origin HEAD:dev
