name: Apply Stage 2c search-data extraction

on:
  push:
    branches: [dev]
    paths:
      - 'tools/apply-stage2c-search-data.mjs'
      - '.github/workflows/apply-stage2c-search-data.yml'

permissions:
  contents: write

jobs:
  extract-search-data:
    runs-on: ubuntu-latest
    steps:
      - name: Check out dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Extract structured search data
        run: node tools/apply-stage2c-search-data.mjs

      - name: Validate resulting application
        run: node tools/check-html.mjs index.html

      - name: Verify Stage 2c.2 structure
        shell: bash
        run: |
          test -s assets/js/search-data.js
          grep -q 'src="assets/js/search-data.js"' index.html
          grep -q 'const CLAUSE_PACK_V13=' assets/js/search-data.js
          grep -q 'const LEGAL_TRAILS_V13=' assets/js/search-data.js
          grep -q 'function clauseKey(' assets/js/app.js
          if grep -q 'const CLAUSE_PACK_V13=' assets/js/app.js; then
            echo 'CLAUSE_PACK_V13 still exists in app.js' >&2
            exit 1
          fi
          git diff --check

      - name: Commit Stage 2c.2 extraction
        shell: bash
        run: |
          git add index.html assets/js/app.js assets/js/search-data.js
          if git diff --cached --quiet; then
            echo 'No Stage 2c.2 changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'refactor: isolate structured legal search data'
          git push origin HEAD:dev
