name: Apply Stage 2f expert extraction

on:
  push:
    branches: [dev]
    paths:
      - 'tools/apply-stage2f-expert.mjs'
      - '.github/workflows/apply-stage2f-expert.yml'

permissions:
  contents: write

jobs:
  extract-expert:
    runs-on: ubuntu-latest
    steps:
      - name: Check out dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Extract expert runtime
        run: node tools/apply-stage2f-expert.mjs
      - name: Validate resulting application
        run: node tools/check-html.mjs index.html
      - name: Verify Stage 2f.2 structure
        shell: bash
        run: |
          test -s assets/js/expert.js
          grep -q 'src="assets/js/expert.js"' index.html
          grep -q 'function analyzeExpert(' assets/js/expert.js
          grep -q 'function renderVerifiedAudit(' assets/js/expert.js
          grep -q 'function syncHomeCleanMode(' assets/js/app.js
          if grep -q 'function analyzeExpert(' assets/js/app.js; then
            echo 'expert runtime still exists in app.js' >&2
            exit 1
          fi
          git diff --check
      - name: Commit Stage 2f.2 extraction
        shell: bash
        run: |
          git add index.html assets/js/app.js assets/js/expert.js
          if git diff --cached --quiet; then
            echo 'No Stage 2f.2 changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'refactor: isolate expert dossier review runtime'
          git push origin HEAD:dev
