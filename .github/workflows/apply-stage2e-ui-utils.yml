name: Apply Stage 2e UI-utils extraction

on:
  push:
    branches: [dev]
    paths:
      - 'tools/apply-stage2e-ui-utils.mjs'
      - '.github/workflows/apply-stage2e-ui-utils.yml'

permissions:
  contents: write

jobs:
  extract-ui-utils:
    runs-on: ubuntu-latest
    steps:
      - name: Check out dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Extract shared UI helpers
        run: node tools/apply-stage2e-ui-utils.mjs
      - name: Validate resulting application
        run: node tools/check-html.mjs index.html
      - name: Verify Stage 2e.2 structure
        shell: bash
        run: |
          test -s assets/js/ui-utils.js
          grep -q 'src="assets/js/ui-utils.js"' index.html
          grep -q 'function toast(' assets/js/ui-utils.js
          grep -q 'function renderWorkspace()' assets/js/app.js
          if grep -q 'function toast(' assets/js/app.js; then
            echo 'shared UI helpers still exist in app.js' >&2
            exit 1
          fi
          git diff --check
      - name: Commit Stage 2e.2 extraction
        shell: bash
        run: |
          git add index.html assets/js/app.js assets/js/ui-utils.js
          if git diff --cached --quiet; then
            echo 'No Stage 2e.2 changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'refactor: isolate shared UI helpers'
          git push origin HEAD:dev
